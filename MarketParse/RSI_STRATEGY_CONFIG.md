# RSI Strategy Configuration

## Overview
????????? ???????? RSI ????????? ???????? ? `appsettings.json` ??? ??????? ????????? ??? ????????? ????.

## Configuration Settings

### appsettings.json
```json
{
  "RSIStrategy": {
    "Period": 14,
    "UpperThreshold": 67,
    "LowerThreshold": 23,
    "AlertCooldownMinutes": 5
  }
}
```

### Parameters

| ???????? | ??? | ?? ????????? | ???????? |
|----------|-----|--------------|----------|
| `Period` | int | 14 | ?????? ??? ??????? RSI (?????? 14) |
| `UpperThreshold` | decimal | 67 | ??????? ????? (???????????????). ????? ??????????? ????? RSI > ????? ???????? |
| `LowerThreshold` | decimal | 23 | ?????? ????? (???????????????). ????? ??????????? ????? RSI < ????? ???????? |
| `AlertCooldownMinutes` | int | 5 | ???????? ????? ???????? ??? ????? ???? (? ???????) |

## How to Change Settings

### 1. Development Environment
???????? ???????? ? `appsettings.Development.json`:
```json
{
  "RSIStrategy": {
    "Period": 14,
    "UpperThreshold": 70,
    "LowerThreshold": 30,
    "AlertCooldownMinutes": 10
  }
}
```

### 2. Production Environment
???????? ???????? ? `appsettings.json` ??? ?????????? ????? ?????????? ?????????:
```bash
RSIStrategy__Period=14
RSIStrategy__UpperThreshold=70
RSIStrategy__LowerThreshold=30
RSIStrategy__AlertCooldownMinutes=10
```

### 3. Docker / Environment Variables
??? Docker ??????????? ??????????? environment variables:
```yaml
environment:
  - RSIStrategy__Period=14
  - RSIStrategy__UpperThreshold=70
  - RSIStrategy__LowerThreshold=30
  - RSIStrategy__AlertCooldownMinutes=10
```

## Examples

### Conservative Strategy (fewer alerts)
```json
{
  "RSIStrategy": {
    "Period": 14,
    "UpperThreshold": 75,
    "LowerThreshold": 25,
    "AlertCooldownMinutes": 15
  }
}
```

### Aggressive Strategy (more alerts)
```json
{
  "RSIStrategy": {
    "Period": 14,
    "UpperThreshold": 65,
    "LowerThreshold": 35,
    "AlertCooldownMinutes": 3
  }
}
```

### Quick RSI (shorter period)
```json
{
  "RSIStrategy": {
    "Period": 9,
    "UpperThreshold": 70,
    "LowerThreshold": 30,
    "AlertCooldownMinutes": 5
  }
}
```

## Implementation Details

### Model Class
`Models/RSIStrategyConfig.cs` ???????? ????????????:
```csharp
public class RSIStrategyConfig
{
    public int Period { get; set; } = 14;
    public decimal UpperThreshold { get; set; } = 67;
    public decimal LowerThreshold { get; set; } = 23;
    public int AlertCooldownMinutes { get; set; } = 5;
}
```

### Service Registration
? `Program.cs`:
```csharp
builder.Services.Configure<RSIStrategyConfig>(
    builder.Configuration.GetSection("RSIStrategy")
);
```

### Usage in Strategy
? `RSISimpleStrategy`:
```csharp
public RSISimpleStrategy(
    TelegramBotService telegramService, 
    ILogger<RSISimpleStrategy> logger,
    IOptions<RSIStrategyConfig> config)
{
    _config = config.Value;
    // ...
}
```

## Monitoring

??? ??????? ??????? ? ????? ????? ???????????? ??????? ????????????:
```
RSI Strategy initialized: Period=14, UpperThreshold=67, LowerThreshold=23, AlertCooldown=5 minutes
```

## Notes

- ????????? ? `appsettings.json` ??????????? ????? ??????????? ??????????
- ??? Production ????????????? ???????????? ?????????? ????????? ??? Azure Configuration
- Cooldown ????????????? ???? ??????? ??? ????? ????
- ??????????? ???????? RSI: 70 (???????????????) ? 30 (???????????????)
- ? ????? ????????? ???????????? ????? ??????????? ????????: 67 ? 23
